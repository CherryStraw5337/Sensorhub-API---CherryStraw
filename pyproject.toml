[tool.pytest.ini_options]
# Pytest solo debe contener los argumentos de configuración propios de pytest. Para cambiar la ruta, solo modifica --cov
# con "--cov={carpeta/archivo que quieres testear}"
addopts = "--cov=app --cov-report=term-missing --cov-fail-under=95"
norecursedirs = ["deprecated", ".git", ".venv", "build", "dist", "__pycache__"]

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
    "migrations",  # Excluye las migraciones de Alembic
    "alembic",     # Excluye la carpeta duplicada si existe
    ".venv",
]

[tool.ruff.lint]
# E: Errores de estilo, F: Errores lógicos, I: Orden de imports, UP: Sintaxis moderna, B: Bugs comunes (flake8-bugbear)
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501", "E203", "B017"]  # Ignora errores de longitud de línea, espacios en expresiones y aserciones de excepciones ciegas

[tool.mypy]
python_version = "3.12"
strict = true
disallow_untyped_defs = true
ignore_missing_imports = true
exclude = ["migrations/", "alembic/", "venv/", ".venv/", "deprecated/"]
plugins = ["sqlalchemy.ext.mypy.plugin"]